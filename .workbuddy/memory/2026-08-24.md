# 2026-08-24 工作日志

## PlayerWarp 插件接入（完成）
- 注册表 `lib/common/plugins.ts` 登记 playerwarp（核心 2 表门槛：warp_player + warp_tp_player，落地 /warp）。
- `lib/plugins/playerwarp/` types/schemas/queries；`app/api/warp/` 5 路由（overview/players/players[uuid]/list/[id]，requirePlugin + Zod）。
- `app/(analysis)/warp/` 5 页（总览/地标库/地标详情/地标玩家/玩家详情）。
- 玩家中心集成：player-registry 来源 + unified 类型 + unified-players 聚合/详情 + 详情页头部摘要与正文区块。
- 修复：类型/服务器分布 SQL 用 `key` 别名未加反引号（MySQL 保留字）→ 改 `` `key` ``；与 `like` 保留字同类问题。全局排查无其他。
- 地标名/类型/描述接入 McText 颜色解析（4 个 warp 页面），玩家名/服务器名保持纯文本。
- README 补 PlayerWarp（功能列表 + 插件表，现覆盖 7 插件）。

## 玩家中心头部摘要排版（完成）
- 头像下方改为分组卡片网格：账户 → 称号 → 任务 → 地标 → 宠物（用户指定顺序）。
- 卡片三行结构：标签行（icon + 名称 text-xs 灰）→ 主值（text-sm 加粗）→ 次值（text-sm 灰 truncate）。
- 称号 usingTitle 用 `block truncate` 单行省略（McText 只接受 { text }，样式包外层 span）。
- 用户要求：卡片内容自适应、不换行、字号不要太小 → 次值统一 text-sm + truncate。

## 开源安全检查（只读，完成）
- .gitignore 覆盖 .env*、/.data/、*.pem、计划.md；179 个跟踪文件无敏感文件名。
- git rev-list --all 对象级扫描：历史提交无 .env/.data/mysql.json/.pem/计划 等路径，干净。
- .env.local 存在且被忽略，从未进过 git 历史；.data/mysql.json 本地不存在。
- auth.ts session 签名密钥从环境变量派生，无硬编码 secret；config.ts MySQL 凭据仅环境变量或 .data/mysql.json（0600）。
- cloud-functions 产物、public/docs、提交信息、ghp_/token 扫描均无敏感内容。
- 唯一注意：README 第 81/88 行 AUTH_PASSWORD/MYSQL_PASSWORD=123456 为示例值（用户主动写入的本地测试配置），建议标注"示例勿用于生产"。

## PlayerCurrency 插件接入（完成）
- 注册 `lib/common/plugins.ts`：playercurrency，门槛表 player_currency + player_currency_log（两表同建，不降级）。
- `lib/plugins/playercurrency/` types/schemas/queries；`app/api/currency/` 4 路由（overview/players/players[uuid]/logs）。
- `app/(analysis)/currency/` 4 页：总览（类型统计+余额排行+最近变更）、流水（keyword/type 筛选+分页）、玩家列表、玩家详情（余额表+最近 50 条流水）。
- 保留字处理：`change` 是 MySQL 保留字，SELECT 里反引号取列（与 warp 的 `like`/`key` 同类）。
- 变更值颜色沿用中国惯例：正数红色、负数绿色。
- 货币类型为插件配置键，纯文本渲染（不走 McText）；玩家名/变更人同样纯文本。
- 玩家中心：registry 用 player_currency LEFT JOIN 流水表 MAX(operator_time) 取最近活跃；unified 列表加 currencyTypes；详情加 playercurrency 摘要（typeCount/topType/topBalance/lastChangeAt）。
- 头部卡片顺序：账户 → 称号 → 任务 → 地标 → 货币 → 宠物（xl 6 列）；正文「货币 · PlayerCurrency」区块在地标区块后。
- README 功能列表 + 插件表补 PlayerCurrency（现 8 插件）。tsc + lint 全过。
